#include "holberton.h"

/**
 * mul - returns int a * int b
 * @a: Input parameter in ASCII
 * @b: Input parameter in ASCII
 *
 * Return: multiplication a * b.
 * On error, -1 is returned, and errno is set appropriately.
 */

int mul(int a, int b)
{

	return (a * b);
}
